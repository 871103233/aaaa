# V0.1 开发计划

- **阶段目标**：验证"**平滑地形 + 可挖可堆 + 胶囊体自由活动**"这条主链路 —— 一个能挖能堆的平滑地形块，外加一个与地表相通的洞穴体积。
- 方向依据：[ADR 0004](../adr/0004-hybrid-layered-world-representation.md)（分层混合世界）；现行方案见 [ADR 索引](../adr/README.md)
- 规则：[SKILL.md](../../.trae/skills/voxel-engine-dev-standards/SKILL.md)
- 状态：进行中（前置条件未清）
- 最后更新：2026-09-25

> - 本文件是**计划与进度**，不写技术结论；选型与理由一律看方案文档与 `docs/adr/`。
> - **验收权威**：`tech-plan-v1.3.md` §8 的 V0.1 验收标准**已随 ADR 0004 作废**；在迁移任务 M1 完成前，
>   **V0.1 的阶段目标与验收以本文件 §2 / §4 为准**（M1 落地后回填 `tech-plan-v2.0.md` §8 并与此处保持一致）。
> - **任务落点**：世界层的目录结构属 v1.3 §9.2、同样被取代，故下表中世界层任务的"落点"为**建议值**，待 M1 定稿后回填。

---

## 1. 前置条件

| # | 前置项 | 状态 | 说明 |
| --- | --- | --- | --- |
| P1 | **M1**：重写方案文档为 `tech-plan-v2.0.md`（含 §0「沿用 / 取代 / 待定」）+ 重写 4 份 `references` | **已完成**（2026-09-25） | v2.0 已落地并成为**现行方案**；4 份 references 已按新方向重写 |
| P2 | 待收敛项 1、2：配置解析方案；可挖标记的产生规则与数据文件格式 | **已完成**（ADR 0005 / 0006） | 配置解析 = toml++（端口 `tomlplusplus`）；标记 = 程序化规则 + `dig_regions.toml` 叠加 |
| P3 | 待收敛项 3：可挖体积的网格化算法 | **已完成**（ADR 0007） | Surface Nets 起步，`int8` 距离场，32³ 体素 / 33³ 采样 |
| P4 | 待收敛项 5：预算与精度口径重算 | **已完成**（ADR 0008） | 尺寸与精度已冻结；Draw Call 与视距内存**只记录、不验收**（待 LOD 收敛） |
| P5 | 依赖补齐 | **已完成**（2026-09-25） | 已加入 `tomlplusplus` / `joltphysics` / `stb` / `imgui[sdl3-binding,sdlgpu3-binding]`，configure 成功；`assimp` 仍后置 |
| P6 | 占位纹理资产（草 / 土 / 岩 / 沙 等 16×16 PNG） | 未开始 | 供 T5 使用；离线打包工具与 Python 留到需要时，须记入 devlog 遗留 |
| P7 | CI 全绿 | 进行中 | run `36146153279` 结果待查。按方案 §9.3「红灯不合并」，**推送前**必须清 |
| P8 | `LICENSE` 占位符 / `NOTICE.md` 标 `*` 条目未核对 | 部分处理 | 本轮已为新依赖补 `NOTICE.md` 行；`LICENSE` 占位符仍开放 |

## 2. 任务分解

| # | 任务 | 落点（目录 / 模块入口） | 验收判据 | 状态 |
| --- | --- | --- | --- | --- |
| M1 | 重写方案为 `tech-plan-v2.0.md`（§0 三张清单 + 世界表示章 + 新路线图 + 新目录结构）并重写 4 份 `references` | `docs/` + `.trae/skills/.../references/` | 索引状态更新为"v2.0 现行"；不存在"两处并存的有效方向" | **已完成**（v2.0 411 行 + 4 份 references 重写） |
| I1 | 修复 Linux CI：runner 预装 vcpkg 工作树有本地改动，`checkout --detach` 被拒 → 改 `checkout -f` | `.github/workflows/ci.yml` | ubuntu asan / tsan 两个 job 通过 `Configure` 与 `Unit tests`（= 清 P7） | 进行中 |
| M2 | **世界层更名**：`voxel/` → `world/`（v2.0 §9.1 定稿的目录结构），并删除已作废的 `voxel/chunk/chunk_types.hpp` 与其测试 | 根 `CMakeLists.txt`、`world/`、`tests/` | 构建与测试全绿；`docs/file-index.md` 与索引同一次提交内同步；无残留引用 `voxel/` | **已完成**（`git mv` 保留历史；6 项旧区块测试随文件删除；门禁默认扫描目录也已由 `voxel` 改为 `world`） |
| M3 | 新增三份**内容基线文档**（内容能力与产出范围 / NPC 行为逻辑 / 世界观设定），并在 SKILL 第六节建立维护义务、占位规则与"先登记后实现" | `docs/` + `.trae/skills/.../SKILL.md` 第六节 | 三份文档存在；未开发项均已**占位**而非留白；维护义务已进 SKILL 同步清单与 DoD | **已完成**（2026-09-25） |
| M4 | **SKILL 新增「需求受理」规则**：先判合理性（不矛盾的更优做法直接执行 / 矛盾则提问且不动代码）+ **默认按业界标准实现**（不等用户点名）+ 与范围控制的边界；并把鼠标锁定、±89° 两个真实案例固化为判定示例 | `.trae/skills/.../SKILL.md` 阅读约定节 + DoD | 规则成文且带判定示例与记录义务；DoD 新增对应自检项；不得与第五节范围控制冲突 | **已完成**（2026-09-25） |
| M5 | **SKILL 新增第七节「品质门槛」**：**功能可用 ≠ 完成**；可见产出必须过品质清单（可读 / 一致 / 整齐 / 名义契约 / 状态可见 / 边界完整）、**第一次交付即达标**、不可自动化的部分须标注"待人工目视验收" | `.trae/skills/.../SKILL.md`（新增第七节；原第七节 DoD 顺延为第八节，原第八节顺延为第九节） | 规则成文并带真实反面案例（UI 首版"功能可用但很丑"）；DoD 新增自检项 | **已完成**（2026-09-25） |
| T18 | **世界边界（空气墙 + 出界救援）**：边界**由地图范围自动推导**（测试地图与以后所有随机地图都生效）；四周围不可穿越的静态墙；另有"掉出世界"救援（越过墙或坠落时送回出生点） | `engine/physics/`（静态盒体）、`world/terrain/`（边界推导）、`game/`（接线） | 走到地图边缘被挡住（不能掉下去）；飞行越过墙后坠落能被送回出生点；边界随 `tile_radius` 变化自动跟随（有纯函数单测） | **已完成**（新增 `world/terrain/world_bounds.*`（纯函数：边界盒 + 4 堵墙放置）+ `engine/physics/PhysicsWorld::AddStaticBox`（通用静态盒体）+ `game/out_of_bounds.hpp`（出界判定纯函数，余量 16 格）；当前地图边界 `(-64,-8,-64)~(128,520,128)`、墙厚 2 格；新增 **10** 项测试，共 **106/106**） |
| T19 | **地表材质过渡观感改进**（人工实测提出"大棱角处过渡色很多"）→ 方向已由 **ADR 0009** 定案：**权重逐像素算 + 分层贴图（albedo + 法线） + 只采权重最高的 3~4 层 + 程序生成占位贴图** | `world/terrain/`（权重与参数）、`assets/shaders/`、`engine/render/`（纹理支持）、程序生成贴图 | 棱角过渡带显著收窄、无宽色带；地表具备材质感（含法线立体感）；纹理参数与 `materials.toml` **同源**；既有 106 项测试全绿 | **已完成**（`world/terrain/material_textures.*` 程序生成 4 层 albedo + 法线（256²、含 mip 约 **2.67 MB** 显存）；`material_table` 升 `schema_version=2`，新增 `MaterialUniform` / `BuildMaterialUniform` 作为 **CPU→GPU 唯一投影入口**；顶点不再承载材质权重，权重在 `mesh.frag` **逐像素**重算（窄带 `smoothstep`）；新增 **7** 项测试，共 **113/113**） |
| T20 | **P0 · HDR 渲染管线**（[ADR 0010](../adr/0010-render-quality-pipeline.md)）：离屏 HDR 目标（`R16G16B16A16_FLOAT` + 深度）→ 后处理通道（**曝光 + ACES 近似色调映射 + sRGB 编码**）→ 交换链；曝光值进配置 | `engine/render/`、`assets/shaders/`（新增 `tonemap.*`）、`assets/config/` | 高光不再"死白截断"、暗部有层次；曝光可调且不需重编 Shader；既有 113 项测试全绿；双格式 Shader 产物齐全 | **已完成**（2026-09-26）（主通道改渲到 `R16G16B16A16_FLOAT` 离屏目标；新增全屏三角形 `tonemap.vert|.frag`：曝光 → **ACES 近似（Narkowicz）** → **分段精确 sRGB 编码** → 交换链；`mesh.frag` 改为**线性空间**着色（albedo 与 tint 转线性、环境 0.10 + 漫反射 1.00）；曝光进 `settings.toml`（可选字段、缺失取默认 1.0、越界钳制 `[0.1, 8.0]`）；叠加层仍在 tonemap **之后**写交换链；双格式产物齐全；新增 3 项测试，共 **116/116**） |
| T21a | **P1 第一步 · 光照配置 + 方向光 + 半球天空光**（ADR 0010）：新增 `assets/config/lighting.toml`（太阳方向 / 强度 / 颜色、天空色 / 地面反弹色），经**单入口投影**进片元 uniform；线性的半球天空光给暗部以天空色（替代写死的 `0.10` 环境项） | `engine/render/`（配置表 + uniform）、`assets/config/lighting.toml`、`assets/shaders/mesh.frag` | 调整配置后观感随之变化且**不需重编 Shader**；背光面呈天空色而不是死黑；既有 116 项测试全绿 | **已完成**（2026-09-26）（新增 `assets/config/lighting.toml`（`schema_version=1`：太阳方向 / 颜色 / 强度、天空天顶 / 地平 / 地面色 + 强度）与 `engine/render/lighting_table.*`（`LightingUniform` std140 **128 字节** `static_assert` + `BuildLightingUniform` **单入口投影**，**颜色在 CPU 侧转线性**）；`mesh_renderer` 新增 `SetLightingUniform` 走**片元 uniform 槽 1**（槽 0 为材质，SDL_gpu 每阶段 4 槽）；`mesh.frag` 删除写死的 `kLightDirection` / 环境常量，改为方向光 + **半球天空光**；清屏色改取配置的地平色线性值；新增 **15** 项测试，共 **131/131**） |
| T21b | **P1 第二步 · 阴影通道 + CSM 级联阴影**（ADR 0010）：无片元的深度通道（光源视角）+ 级联划分与包围盒拟合 + 片元侧级联选择与 PCF + 深度偏移；级数与分辨率**进配置** | `engine/render/`（阴影通道与光矩阵）、`assets/shaders/`（`shadow.vert` 等）、`assets/config/lighting.toml` | 地形起伏与地标塔产生**可见且不抖动的阴影**；无 acne / peter-panning；**全屏 / 阴影通道的 UV 与坐标约定须遵守 `references/meshing-and-render.md` §4** | **已完成**（2026-09-26）（`lighting.toml` 升 `schema_version=2` 新增 `[shadow]`（级数 3 / 2048² / 覆盖 180 格 / λ 0.75 / 偏移）；新增纯函数 `engine/render/shadow_cascade.*`（分割 + **texel 对齐**的光空间矩阵 + `ShadowUniform` 304 B）与 `D32_FLOAT` **深度数组** + 深度通道；`mesh.frag` 增 `sampler2DArray` 采样 + 级联选择 + **3×3 PCF** + 法线偏移；阴影图 **48.00 MB** 已计入 `RenderStats::textureBytes`；新增 **23** 项测试，共 **154/154**。**观感待人工目视**） |
| T21c | **P1 第三步 · 指数高度雾**（ADR 0010）：按视距的指数高度雾，雾色取自天空色（保证远景与天空融合）；密度 / 高度衰减进配置 | `assets/shaders/mesh.frag`、`assets/config/lighting.toml` | 远景自然融入天空、无硬边；近景不被雾洗白 | **已完成**（2026-09-26）（`mesh.frag` 在**光照之后、写 HDR 之前**在线性空间施加指数高度雾：`fogAmount = 1 - exp(-density · 视距 · exp(-heightFalloff · 相对相机高度))`，雾色默认取 `sky.horizon_color`；`fog.enabled = false` 时整体跳过，不留无用运算；参数全部来自 `lighting.toml`。**观感待人工目视**） |
| T22 | **P2 · PBR 与材质四件套**（ADR 0010）：Cook-Torrance（GGX + Smith + Schlick）；每层 **albedo / normal / roughness / AO** + **宏观变化**；程序生成升级为**多尺度**；`materials.toml` 升 `schema_version=3` | `world/terrain/material_textures.*`、`material_table.*`、`assets/config/materials.toml`、`assets/shaders/mesh.frag` | 岩石有方向性高光、粗糙度可分辨；CPU/GPU 参数**同源**（有单测）；纹理显存记账更新 | **已完成**（2026-09-26）（`materials.toml` 升 **`schema_version=3`**：每层新增 `roughness` / `ao` / `macro_uv_scale` / `macro_strength`；程序生成**双频段** albedo/normal + **新增 roughness（4 层）/ AO（4 层）/ macro（1 层）**三张数组（材质总计 **5.67 MB**）；`MaterialUniform` 扩到 **272 字节**（≤ 512 上限）仍经 `BuildMaterialUniform` 单入口投影；`mesh.frag` 改为 **Cook-Torrance（GGX + Smith + Schlick，F0 = 0.04）**，**AO 只作用环境项**、宏观变化调制 albedo 与 roughness，四件套采样**仍在"仅权重最高 3~4 层"守卫内**；`set=2` 绑定槽 0~5；新增 **9** 项测试，共 **163/163**。**观感待人工目视**） |
| T23 | **P3 · 抗锯齿与近景细节**（ADR 0010）：**MSAA 4×**（sample count + resolve）+ 多频细节法线 | `engine/render/`、`assets/shaders/` | 边缘无可见锯齿、远处地形不闪烁；帧率仍满足方案 §7.2；开关 MSAA 有可对比数据 | **已完成**（2026-09-26）（`msaa_samples` 进 `settings.toml`（**合法档 {1,2,4,8}**，默认 4，**并列时向上**钳制；缺失取默认、类型错报错）；主通道渲进 `R16G16B16A16_FLOAT` **多采样目标**后以 **`SDL_GPU_STOREOP_RESOLVE`** 解析到单采样 HDR 目标（**档位 = 1 时不建 MSAA 纹理，零额外开销**），阴影通道保持 1×；MSAA 颜色与深度目标**均计入显存记账**；`mesh.frag` 增**多频细节法线**（第二频段 UV ×4、**RNM** 合成、**仅最高权重层**，避免带宽翻倍）；新增 **2** 项测试，共 **165/165**。**观感待人工目视**） |
| T25 | **陡壁三平面混合投影（triplanar）**（项目所有者批准实施，并要求**随地形改变自动切换**）：按坡度在平面投影与三平面之间**逐像素混合**，平坦处保持单次采样 | `assets/config/materials.toml`（`[triplanar]`）、`world/terrain/material_table.*`、`material_blender.*`、`assets/shaders/mesh.frag` | 陡壁纹理不再竖向拉伸；**地形被挖 / 堆后自动跟随（无烘焙、无重建、无 CPU 分支）**；平地片元采样次数**不增加**；混合权重有 CPU 纯函数单测 | **已完成**（2026-09-26）（`materials.toml` 增全局 `[triplanar]`（`enabled` / `slope_min` 0.45 / `slope_max` 0.70 / `sharpness` 4.0，非法即抛异常）；`TriplanarBlendWeight(slope, …)` 为 CPU 纯函数并与着色器**逐字镜像**；`MaterialUniform` 272 → **288 字节**；**只对权重最高的一层、只对 albedo 与 normal** 做三平面（roughness / AO / macro 保持平面 —— 陡壁视觉影响小）；平地 `triWeight ≤ ε` **早退**到原路径（**采样次数不变**），最坏（陡面）每片元由 21 → **25** 次（+19%）。权重由**逐像素世界法线**算出 ⇒ 笔刷挖 / 堆后**无需任何额外动作**） |
| T24 | **F1 面板性能开销数据**（项目所有者追加，2026-09-26）：**Draw Call / 三角形数 / 纹理显存 / CPU 帧时间分解 / 各 pass GPU 时间** | `game/debug_overlay.*`、`engine/render/` | 面板显示上述数字且与实测一致；纯展示（不阻塞主线程、不改模拟状态）；隐藏时开销近似为零 | **已完成**（2026-09-26）（引擎层新增通用 `RenderStats`（`drawCalls` / `triangleCount` / `vertexCount` / `textureBytes`）+ 纯函数 `EstimateTextureArrayBytes`（含 mip 的 4/3 估算），显存在纹理 / 目标创建与释放时增减；F1 面板新增 Draw Call / 三角形 / 顶点 / 纹理显存 / **CPU 三相耗时（逻辑步 / UI 构建 / 渲染提交，ms）**；**GPU pass 时间因 SDL3_gpu 无时间戳查询 API，面板显式标注"不可用"，未编造数字**） |
| B7 | **缺陷修复：高处平地一律被涂成草色 + 平台边缘宽棕环**（人工实测第 5 轮："颜色特别不自然"） | `assets/config/materials.toml`、`world/terrain/material_table.*` | 任意 `(高度 ∈[0,512], 坡度 ∈[0,1])` 至少一层权重非零（有**不变量单测**）；平台平地呈**单一自然色**而非多色混涂 | **已完成**（2026-09-26）**契约**：权重归一化的"无匹配 → 槽位 0"兜底**只应给异常输入**，不得在正常地形上大面积触发。**根因（机制）**：旧带在 **高度 120、坡度 0** 处四层隶属度全为 0（草 `1−smoothstep(96,112,120)=0`、土 `smoothstep(0.05,0.20,0)=0`、岩 `smoothstep(0.30,0.45,0)=0`、沙 `0`）⇒ 落兜底 ⇒ 整片平地强制草色；"土环"来自土的中坡带、"岩边"来自岩的坡度带。**修复**：重调带参数使覆盖完备，并把**岩改为纯坡度驱动**（坡度带 `[0.55,1.0]`、**不含 0**、高度带覆盖全域）—— 于是平台平地是**纯草**（草 1.0、土/岩 0）、坡沿是"草 →（窄过渡）→ 灰岩"、**低海拔陡壁同样是灰岩**（不再是土色）；**明确取舍**：高海拔**平地**由土（碎石土）接管（"高海拔偏土"，而非旧版"高海拔偏岩"），因为"高处平地呈岩"必须让岩的坡度带包含 0，那会把平台涂成草岩灰。**新增不变量单测**（全域网格：覆盖量 > 阈值）在修前**确实失败**（`height=0 slope=0.76` 处为 0），修后通过；带参数论证写在 `materials.toml` 文件头（含"最容易被改坏的一条"警示） |
| T1 | 固定步长主循环 + 累加器 + 单调计时 + 统一日志接口 | `engine/core/` | 单测：30 / 144 FPS 下逻辑步数一致；单帧补步上限 5 生效 | **已完成**（`FixedStep.*` 4 项 + `Clock.*` 1 项通过） |
| T2 | 动作状态输入层（持续按下 / 本帧按下），每帧采样一次 | `engine/input/` | 单测：同帧多个逻辑步不重复消费同一次点击 | **已完成**（`InputMap.*` 6 项通过） |
| T3 | 网格渲染路径（顶点/索引缓冲、材质权重、相机 UBO）+ **第三人称相机（含避障）** | `engine/render/` | 能渲染测试网格并随相机移动；相机不穿地形 | **已完成**（相机 6 项单测通过；`mesh.vert` / `mesh.frag` 已加入 `assets/shaders/` 并注册，双格式产物齐全） |
| T4 | 地表高度场 tile：数据布局 + 多层噪声生成 + 平滑网格化 + 法线 | `world/terrain/` + `world/generation/` | 单测：相邻 tile 边界**几何连续无裂缝** | **已完成**（4 项单测，含共享边界逐位相等；FastNoiseLite 1.1 已 vendored 到 `third_party/`） |
| T5 | 地表材质：多纹理权重混合（按高度 + 坡度） | `world/terrain/material_*` + `assets/config/` | 陡坡为岩、平地草土过渡自然、**无渗色** | **已完成**（6 项单测：权重归一、陡坡偏岩、低平偏草、配置非法即报错；纹理仍是**占位色**，真纹理待后续） |
| T6 | 笔刷挖掘 / 堆建 + 局部重网格 | `world/dig/` + `world/terrain/` | 操作后 **≤ 1 帧**可见更新；单次笔刷只重网格受影响 tile | **已完成**（6 项单测：半径内逐列变化、半径外不变、脏 tile 精确、垂直范围钳制；运行期只重传脏 tile） |
| T7 | 角色：Jolt `HeightFieldShape` 地形碰撞 + `CharacterVirtual` 胶囊（走 / 跳 / 上坡 / 上台阶） | `engine/physics/` + `world/terrain/` + `game/` | 20 格/秒冲刺不穿地形；1 格台阶可自动上步 | **已完成**（5 项无窗口物理测试：落地静止、上 1 格台阶、冲刺不穿台阶 / 不穿 7 格墙；常量 重力 24 / 跳跃 8 / 半径 0.30 / 总高 1.80 / 最大坡度 50° / 自动上台阶 1.0） |
| T8 | 最小可挖体积：局部 SDF + 等值面网格化 + **与地表相接的洞口过渡** | 世界层（建议 `world/dig/`） | 体积内可任意方向挖掘；交界处**无裂缝、无穿模** | 未开始 |
| T9 | 调试设施：ImGui 面板（帧时间 / 坐标 / 笔刷半径）+ 极简 CVar | `game/` | 面板可随时开关且不崩溃；不进入世界层热路径 | **已完成**（F1 开关；帧时间 P50/P95、FPS、固定步数、角色位置、相机、笔刷半径、tile/脏 tile 计数；每帧零堆分配。**已知限制：面板为只读**——`Window::pump_events` 独占事件队列，未转发给 ImGui 后端，故不可拖动/输入） |
| T10 | 阶段验收：按本文件 §4 逐项实测 | 本文件 §4 | §4 全维度绿 + `ctest --preset debug` 全绿 + 门禁脚本退出码 0 | 未开始 |
| T11 | **预设固定地图**：`assets/maps/<name>.toml`（种子 + 地形编辑区 + 出生点）与加载路径，随附 1 张手工测试地图 | `assets/maps/`、`world/`、`game/` | 单测：合法 / 非法预设解析、编辑区高度符合预期、出生点在地表之上；运行后确实进入该地图 | **已完成**（`MapPreset.*` 3 项通过；示例地图 `assets/maps/test_range.toml` 含出生平台 / 阶梯坡道 / 1 格台阶 / 陡坡 / 深坑 / 地标塔，共 19 条编辑） |
| T12 | **主角可测试化**：出生点由地图定义 + **飞行模式**开关（便于俯瞰与快速移动） | `game/` | 运行期可切换飞行且不崩溃；按键说明写入日志 | **已完成**（出生点 = 地表 + 0.5 格间隙；`F` 切换飞行、`Space` 升、左 `Ctrl` 降；启动即打印完整控制说明） |
| B1 | **缺陷修复：A/D 左右反向**（人工实测第 1 轮发现） | `game/` | A = 相机左、D = 相机右；**W/S 一并复核**；移动基向量抽为纯函数并加单测（以相机右向量为基准断言） | **已完成**（根因：`right` 写成 `(cos yaw, 0, -sin yaw)`，恰为 `-cross(forward, up)`；改为显式叉乘；`CharacterMovement.*` 2 项单测以相机基向量为权威断言四个方向） |
| T13 | **主角可视化**：主角目前只有物理胶囊、**没有任何可视网格**，人工实测"看不到自己" | `game/`（复用 `engine/render/mesh_renderer.hpp`） | 运行后能明显看到自己的角色随移动 / 转向移动；不破坏性能与既有 42 项测试 | **已完成**（程序化胶囊网格 `BuildCapsuleMesh`，尺寸同碰撞胶囊，材质权重压在沙色槽以求强对比；每帧就地刷新顶点，稳态零堆分配；`CharacterMesh.*` 2 项单测） |
| B2 | **缺陷修复：右键堆地形把画面卡成一片蓝**（人工实测第 2 轮；画面只剩清屏色，疑为"几何失效"或"每帧重网格风暴"） | `game/` + `world/dig/`（按实测根因） | 长按右键连续堆地形 ≥ 10 秒不卡死、画面正常；高度不越界、顶点全部有限、索引合法（须有**回归单测**）；根因须有证据，不许猜 | **已完成**（**两个候选假设均被实验排除**。真根因：地表被抬到注视点之上 → 避障 `safeT=0` → 跟随距离压到 0 → `eye==target`，再被"离地间隙"安全网顶到正上方 → 视线与上方向平行 → `lookAt` 基向量归一化得 NaN → 视图矩阵全 NaN → 顶点全剔除，**只剩清屏色（浅蓝）**；修复：`kCameraMinDistance=0.5` 托底最小跟随距离。**同源第二问题**：堆土把角色埋进静态高度场后 `CharacterVirtual` 不被顶出 → 支撑失效 → 穿地坠落；修复：`SetCharacterPosition` 顶回新地表。新增 4 项回归测试） |
| B3 | **缺陷修复：空格跳跃无效**；并按设计规则把**跳跃高度定为角色身高的 60%**（当前 1.80 → **1.08 格**），由身高与重力**推导**初速，不得写死 | `game/` | 地面按空格能起跳；实测最高点 ≈ 1.08 格（单测以重力/初速推导的 apex 断言，误差 < 5%）；飞行模式下空格仍为上升 | **已完成**（**4 个候选假设均不成立**。真根因：空格"本帧按下"边沿在**帧边界被消费**，却只在**固定步循环**里生效；本机关闭垂直同步（Mailbox）后跑到 ~1400 FPS，**96% 的帧 `steps==0`，边沿被静默丢弃**；修复：按下边沿**帧级锁存**后再交给固定步。跳跃初速改为 `sqrt(2·24·0.6·1.80) = 7.20`，最高点 1.08 格。新增 3 项测试） |
| B4 | **待确认现象**：无预设地图的噪声地形上，角色静止高度与 `QueryHeight` 不一致（有预设地图时完全一致） | `world/terrain/`（待定） | 先复现并定位，再决定是否修 | 未开始（人工实测第 2 轮旁路发现，超出当次范围，**登记以免丢失**） |
| B5 | **缺陷修复：P0 落地后画面上下翻转**（人工实测第 4 轮报"视角不对"） | `assets/shaders/tonemap.vert` | 世界不再上下倒置；UV 约定有注释写明依据 | **已完成**（2026-09-26）**根因**：SDL_gpu 的 **NDC 为"+Y 向上"**、**纹理坐标为"+Y 向下"**（`SDL_gpu.h` §Coordinate System），而 `v_uv = position * 0.5 + 0.5` 把屏幕**上方**映射到图像**底部** ⇒ 整帧垂直翻转；叠加层在 tonemap **之后**直接写交换链，故表现为"**UI 正常、世界倒置**"。**修复**：`v_uv.y = 0.5 - position.y * 0.5`；并已把该约定写进 `references/meshing-and-render.md` 与学习笔记，防复发 |
| B6 | **缺陷修复：超高投射体的阴影随视角变化**（人工实测第 5 轮） | `engine/render/shadow_cascade.*`、`assets/config/lighting.toml`、`game/main.cpp` | 相机转动时阴影**不再改变**；有回归测试拄死"高大投射体必须落在各级光空间盒内" | **已完成**（2026-09-26）**契约**：阴影是几何与光照的函数、不应随相机朝向变化（本文件 T21b 验收"可见且不抖动"）。**根因（机制）**：级联正交盒只包住"视锥切片 AABB 的外接球"（`±radius` × `0~2·radius`），而阴影管线开 `enable_depth_clip` ⇒ **高于该球的地标塔顶部被裁掉**，只有落在盒内的那段塔身参与投射；相机一动 ⇒ 切片变 ⇒ 盒变 ⇒ 参与投射的塔身段变了。**修复**：按 **[ADR 0010](../adr/0010-render-quality-pipeline.md) 的 shadow caster extension** 扩展光空间盒（沿光轴远端前移 `casterHeight/\|dir.y\|`、X/Y 各扩 `casterHeight·tanθ`），`casterHeight` 由 `TerrainWorld::MaxSurfaceHeightBlocks()` **自动推导**（每级独立取 `max(配置下限, 最高地表高度 − 该级切片中心高度)`），`lighting.toml` 新增 `caster_height_min`（160 格）作兜底。**修复前回归测试确实失败**（`coveredClip.y = 4.07 > 1`、`coveredClip.z = -2.29 < -1`），修复后通过；既有"亚 texel 平移不改变矩阵 / 包围球 8 点在内 / 每级覆盖视锥切片"断言**一字未动**。**代价**：近景级联覆盖面变大 ⇒ 阴影分辨率摊薄（固有取舍，可调 `resolution` 或 `caster_height_min`） |
| T16 | **UI 可读性与外观**：① 修掉两个面板的 **`???`**（根因：ImGui 默认字体无 CJK 字形）——加载 CJK 字体，**取不到字体时自动改用纯英文标签，任何机器上都不得再出现 `?`**；② 应用统一**主题样式**并重做两个面板的**布局**（分区 / 对齐 / 间距 / 尺寸） | `game/`（UI 层）、`assets/fonts/`（可选内嵌字体位） | 面板上**不出现任何 `?` 缺字**；无 CJK 字体的机器回退英文且**有单测钉死**（回退时标签全为 ASCII）；两个面板视觉统一、控件分组清晰；不新增功能控件，78 项测试全绿 | **已完成**（新增 `game/ui_text.hpp`（**标签缝**）+ `ui_font.*`（**三级字体解析**）+ `ui_theme.*`（唯一主题入口）；本机解析到 `C:\Windows\Fonts\msyh.ttc` ⇒ 中文生效；无字体机器整表回退英文；新增 **9** 项测试，共 **87/87**；其中一项**扫描面板源码禁止绕过标签缝**） |
| T14 | **视角控制改进（G8）**：鼠标**相对模式锁定**（光标隐藏、位移不再受屏幕边界限制、不移出窗口）+ 俯仰**确实可达 ±89°**；`Esc` 释放、点击重新捕获（该次点击**不得**同时触发挥/堆）、失焦自动释放 | `engine/platform/`、`game/` | 鼠标移到屏幕边缘视角仍继续转；上下能到 ±89°（单测：反复大幅 `AddPitch` 后钳制恰为 ±89° 且**视图矩阵有限**）；释放/重捕获逻辑有日志且不误触笔刷 | **已完成**（`Window::SetRelativeMouseMode` 幂等封装 + 失焦自动释放；纯函数状态机 `DecideMouseCapture` 钉死"重捕获点击先于笔刷判定被消费"；新增 6 项测试；面板新增"鼠标捕获"一行） |
| T15 | **第一个界面：ESC 系统面板**——显示模式（窗口 / 全屏）、窗口下的**分辨率选择**、**音量 0–100 滑块**、**退出游戏按钮（真的退出）**；配套 `docs/ui-inventory.md` 界面清单 | `game/`、`engine/platform/`、`assets/config/`（设置持久化） | 面板可开关且**可交互**（须先补 ImGui 事件转发）；退出按钮真的结束进程；全屏/分辨率真的作用于窗口；音量值持久化并在文档标注"当前无声源"；**ESC 职责统一为"开关面板"**（与 T14 的释放语义合并，不冲突） | **已完成**（新增 `engine/platform/settings.*`（TOML，落盘 `SDL_GetPrefPath`）+ 通用事件转发回调（`engine/` 不依赖 ImGui）+ `game/system_panel.*` + `game/gameplay_input.hpp`（输入抑制纯函数）；`Esc` 统一为开关面板，打开释放捕获、关闭恢复；新增 **17** 项测试，共 **78/78**；退出码 0 且退出前保存设置） |

**明确不做（防范围蔓延）**：

- **全世界三维体素**（ADR 0004 已明确不采纳）。
- **程序化浮空岛 / 遗迹、玩家自由搭建**：本阶段**只做飞行元素**（实体层），浮空与建造后置。
- **人物模型与骨骼动画**：本阶段只用**胶囊体占位**。
- 后置阶段项：LOD、遮挡剔除、存档落盘、Jolt 动态刚体、粒子与音频。
- SKILL「范围控制」表中的五项暂缓项（场景编辑器、脚本系统、网络联机、多渲染后端、自研 ECS）**一项都不启动**。

## 3. 当前进度

- **已完成**
  - 决策层：ADR 0001~**0010** + [ADR 索引](../adr/README.md)；世界表示 = 分层混合（ADR 0004）；待收敛项 1/2/3/5 已由 ADR 0005~0008 **收敛关闭**；地表材质管线（逐像素权重 + 分层贴图）由 **ADR 0009** 定案；**渲染质量线（HDR → 光照 → PBR → AA）由 [ADR 0010](../adr/0010-render-quality-pipeline.md) 定案并按项目所有者授权立项**
  - 方案层：**M1** —— `tech-plan-v2.0.md`（现行方案，含 §0 沿用/取代/待定）+ 4 份 `references` 按新方向重写
  - 规范层：SKILL 红线 / 口径表 / 待收敛项 / DoD / 「方案与方向的留档义务」；门禁默认扫描目录已随 M2 由 `voxel` 改为 `world`
  - 依赖：`tomlplusplus` / `joltphysics` / `stb` / `imgui[sdl3-binding,sdlgpu3-binding]` 已入 `vcpkg.json`；**FastNoiseLite 已 vendored** 到 `third_party/`（MIT）
  - **M2**：世界层更名 `voxel/` → `world/`（保留历史），删除已作废的 `voxel/chunk/chunk_types.hpp` 与 6 项旧测试
  - **T1~T6 全部完成**：`engine/core`、`engine/input`、`engine/render`（网格渲染路径 + 第三人称相机 + `ITerrainQuery` 契约）、`world/terrain`（高度场 tile / 网格化 / 材质混合 / `ITerrainQuery` 实现）、`world/generation`（确定性噪声）、`world/dig`（笔刷 + 脏 tile）
  - **T7~T19 全部完成**：`engine/physics`（Jolt 高度场 + `CharacterVirtual` 胶囊）、`engine/platform/settings`（设置持久化）、`engine/core/frame_limiter`（零忙等限帧）、`game/`（系统面板 / 字体与标签缝 / 主题 / 鼠标捕获 / 出界救援）、`world/terrain`（世界边界、**材质贴图管线**）
  - **质量线 P0 已完成**：[ADR 0010](../adr/0010-render-quality-pipeline.md) 的第一步落地 —— **HDR 离屏 + ACES 色调映射 + sRGB 编码**（T20）与 **F1 面板性能开销数据**（T24）
  - **P1 全部完成（T21a / T21b / T21c）**：光照配置表 + 方向光 + 半球天空光；**阴影通道 + CSM 级联阴影**；指数高度雾
  - **P2 已完成**：**T22** PBR（Cook-Torrance）+ 材质四件套（albedo / normal / roughness / AO）+ 宏观变化 + 多尺度程序生成（材质 5.67 MB）
  - **P3 已完成 → 渲染质量线 P0~P3 全部落地**：**T23** MSAA 4×（`RGBA16F` 多采样 + resolve，档位可配）+ 多频细节法线
  - **待收敛项 10 已收敛**：显存与带宽记账回填至方案 §7.2.1 / §7.2.2（含 P1 / P3 增量预估）；记账做成**可观测**——启动日志新增 `GPU 纹理显存记账：…` 一行，并计入 F1 面板
  - **人工实测第 5 轮的两个缺陷 + 一项已登记限制已闭环**：**B6** 阴影随视角变化（级联投射体扩展）、**B7** 材质带空洞（高处平地"假草"+ 宽棕环）、**T25** 陡壁三平面（按坡度自动切换）
  - 构建与测试：零错误零警告；`ctest --preset debug` → **176/176 passed**；门禁 `scanned 88 file(s), 0 violation(s)`
  - **可运行**：`voxel_game.exe` 冒烟通过 —— 9 个 tile 生成并上传、`mesh.*` 双格式 Shader 四产物齐全、材质贴图（256² × 4 层，含 mip 约 2.67 MB）生成并上传、运行期 `Responding=True`；左右键挖 / 堆（笔刷半径 6 格）只重传脏 tile
  - CI：Windows 的 debug / release **均已转绿**（含新依赖与全部测试）；Linux 见 I1
- **下一步**
  1. **人工目视验收（渲染质量线 P0~P3 + B6 / B7 / T25）**：看测试地图 —— ① 曝光与对比自然、无死白死黑（T20）；② 背光面呈天空色而非死黑、远景融入天空（T21a/c）；③ **相机转动时高柱阴影不再变化**、无 acne、无可见分离（T21b / **B6**）；④ **陡坡岩壁掠射角有可分辨高光**（T22）；⑤ 边缘无锯齿、远处不闪烁（T23）；⑥ **平台平地是一条自然的绿（不再是多色混涂）**、坡沿不见宽棕环、**陡壁侧面纹理不再竖向拉伸**（**B7 / T25**）；⑦ 世界不上下倒置（B5）；⑧ F1 面板数字合理
  2. **T8**：最小可挖体积（局部 SDF + Surface Nets + 与地表相接的洞口过渡）
  3. **T10**：阶段验收（按本文件 §4 逐项实测）
  4. 收口 **I1**：Linux CI（真实错误为 `libxcrypt` BUILD_FAILED，级联误报为 Ninja / 编译器未设）+ Windows CI（MSVC 14.51 报 `map_preset.cpp` C4864，需补 `template` 关键字）
  5. **T19 遗留两处待决**（见 devlog "T19 地表材质管线"条目「遗留」）：① 主角胶囊不再有固定沙色外观（现与地表共用同一片元材质）；② `texture_layer` 自 `1` 起、但实现按 `textureIndex = texture_layer - 1` 落在 **4 层**数组上，与 `references/meshing-and-render.md` §3「`index = 0` 保留给缺失纹理」的口径**尚未对齐**（要么数组补 1 层占位，要么改口径，须经确认）
  6. **质量线后续可选**（ADR 0010 的切换条件，**均须另开 ADR 或按切换条件重新评估**）：SSAO / 视差遮蔽 / RVT / 后处理 AA；以及网格缓冲接入运行期显存记账
- **阻塞 / 未决**
  - 待收敛项 **4（地表 LOD）、6（遮挡剔除）** 仍开放；**7（物理层的世界坐标精度：`joltphysics` 单精度 `RVec3` 与红线 6 的 `double` 世界坐标冲突）**；**10（渲染质量线显存与带宽）已于 2026-09-26 收敛**（方案 §7.2.1 / §7.2.2，各阶段落地时须复核）；收敛前 Draw Call 与视距内存**只记录、不验收**
  - 世界层目前只覆盖 ① 高度场地表与角色碰撞；② 可挖体积（T8）、③ 物件层、流式加载均未建
  - 环境提示：本机 shell 默认无 MSVC 开发环境，构建须在同一条命令内先初始化 VS DevShell（否则 `C1083 "无法打开包括文件 cstdint"`）

## 4. 阶段验收对照

| 验收维度 | 状态 | 依据 |
| --- | --- | --- |
| 地表**平滑无方块感**（目视） | 未开始 | ADR 0004 |
| 地表可挖可堆（坑 / 土堆 / 平整） | 未开始 | 同上 |
| 可挖体积内自由三维挖掘 | 未开始 | 同上 |
| **层间过渡**：体积与地表交界无裂缝、无穿模 | 未开始 | 同上 |
| 角色移动：走 / 跳 / 上坡 / 上台阶，不穿地形 | 未开始 | 同上 |
| 第三人称相机（含避障） | 未开始 | 同上 |
| 挖掘后 **≤ 1 帧**可见更新 | 未开始 | 同上 |
| 调试设施可用且不崩溃 | 未开始 | 同上 |
| 工程门禁：一键构建 + 零警告 + 测试与门禁全绿 | 未开始 | 同上 |
| 性能与内存达标 | 未开始 | **待收敛项 5**（收敛前不设数字，禁止引用旧预算） |

**追加质量线验收**（项目所有者于 2026-09-26 追加，非方案 §8 原有维度）：

| 验收维度 | 状态 | 依据 |
| --- | --- | --- |
| T20 高光不再"死白截断"、暗部有层次（HDR + 色调映射） | **已完成**（代码链路；**观感待人工目视**） | [ADR 0010](../adr/0010-render-quality-pipeline.md) |
| T21 地形起伏产生可见阴影；暗部为天空色不是死黑；远景与天空无缝 | **已完成**（代码链路 + 单测；阴影 / 闪烁 / acne 等**观感待人工目视**） | 同上 |
| T22 岩石有方向性高光、粗糙度可分辨；CPU/GPU 参数同源 | **已完成**（代码链路 + 单测；高光/粗糙度观感**待人工目视**） | 同上 |
| T23 边缘无可见锯齿、远处地形不闪烁，且帧率仍达标 | **已完成**（代码链路 + 单测；锯齿 / 闪烁**观感待人工目视**） | 同上 |
| T24 F1 面板可读出 Draw Call / 三角形 / 纹理显存 / CPU 帧时间分解 | **已完成**（GPU pass 时间因 API 缺失显式标注"不可用"） | 同上 |
| **B6** 相机转动时高柱阴影**不再变化** | **已完成**（回归测试钉死；观感待人工目视） | 同上 |
| **B7** 平台平地呈**单一自然色**（不再多色混涂） | **已完成**（不变量单测 + 带参数论证；观感待人工目视） | 同上 |
| **T25** 陡壁侧面纹理**不再竖向拉伸**，且随地形改变**自动跟随** | **已完成**（CPU 纯函数单测；观感待人工目视） | 同上 |
