# 游戏设计（本项目要做的游戏）

> **性质**：本仓库的**内容基线文档**之一（见 `.trae/skills/voxel-engine-dev-standards/SKILL.md` 第六节）。
> **层面**：**游戏层** —— 这里写"**我们要做哪款游戏、要求什么**"，与"引擎能做什么"分开。
> 引擎的通用能力见 [引擎能力与产出范围](engine-capabilities.md)；世界背景见 [世界观设定](world-setting.md)；
> NPC 见 [NPC 行为逻辑](npc-behavior.md)。
> **维护义务**：任何一次改动**新增、修改或移除游戏需求 / 玩法规则 / 阶段范围**，必须**同一次提交内**更新本文件。
> 未实现的需求**先占位**（状态 `未开始`），不得留白。

## 1. 这是一款什么游戏

**修仙题材的开放大世界游戏**（当前阶段的全部需求见 §2）。

| 项 | 结论 | 依据 |
| --- | --- | --- |
| 题材 | **修仙大世界** | 项目所有者确认（2026-09-25） |
| 视角 / 控制 | 第三人称，玩家控制自己的角色 | 同上；引擎侧已有第三人称相机与胶囊角色 |
| 世界尺度 | **大世界**（尚未给具体数值） | 待补：具体尺度过大或过小都会影响流式与 LOD 参数 |
| 商业模式 / 平台 | 待补 | 待项目所有者提供 |

## 2. 当前阶段的全部需求（所有者已确认）

> 这是**当前阶段**的范围。每行都必须是可判定的；未实现的写 `未开始`，不得空着。

### 2.1 世界生成（三种并存的要求）

| # | 需求 | 状态 | 实现方式 / 落点 | 备注 |
| --- | --- | --- | --- | --- |
| G1 | **每次进入世界都随机生成**（同一种子可复现，不同种子世界不同） | **已实现** | 种子 + 整数坐标的**纯函数**生成（`world/generation/`） | 随机即可复现：换种子即是新世界 |
| G2 | **可对必备元素进行定义**（声明"这个世界必须有 X"） | **未开始** | 尚无实现 | 需要一份**世界需求清单**（必备元素 + 出现条件），先生成需求再生成世界；格式与规则**待定** |
| G3 | **可预设固定地图**（手工地图，用于测试 / 剧情） | **已实现** | `assets/maps/<name>.toml`（种子 / 范围 / 出生点 / 地形编辑区）；示例 `assets/maps/test_range.toml` | 手工编辑域：flatten / raise / carve |

### 2.2 玩家能力

| # | 需求 | 状态 | 实现方式 / 落点 | 备注 |
| --- | --- | --- | --- | --- |
| G4 | **能跑** | **已实现** | 走 / 冲刺（`game/`，胶囊控制器） | — |
| G5 | **能飞** | **部分实现** | 飞行模式开关（`game/`） | 当前是"可直接操控升降"的测试设施；**是否作为正式玩法（灵力 / 御剑等）尚未定**，待世界设定确认 |
| G6 | **能破坏地形** | **部分实现** | 球笔刷挖 / 堆 + 脏 tile 重网格（`world/dig/`） | 可改地形高度；**三维挖掘（洞穴、横向掏洞）未实现** |
| G7 | **能在大世界里收集东西** | **未开始** | 尚无实现 | 需要：可收集物（物品定义）、拾取交互、背包 / 持有；物品系统整体未建 |
| G8 | **视角控制**：以水平面为基准**上下各看近 90°**（规格 **±89°**，留 1° 防退化矩阵）；**鼠标锁定**（相对鼠标模式，光标隐藏、位移持续输入、不会移出窗口） | **已实现** | 平台层相对鼠标模式（失焦自动释放）+ `game/` 捕获状态机（`Esc` 释放 / 点击重捕获）+ 相机俯仰钳制 | 俯仰上限**不得取正好 90°**（视线与上方向平行会让 `lookAt` 得 NaN，见 B2）；重捕获的那次点击**不得**同时触发挥/堆 |
| G9 | **系统面板与设置**：`Esc` 打开面板，含**显示模式（窗口 / 全屏）**、窗口下的**分辨率选择**、**音量 0–100**、**退出游戏**；设置须持久化（下次启动仍生效） | **已实现** | `game/system_panel.*`（面板）+ `engine/platform/settings.*`（设置与落盘）+ `engine/platform/window.*`（显示模式 / 分辨率 / 事件转发）+ `game/gameplay_input.hpp`（输入抑制） | **控件标签即行为契约**，见 [ui-inventory.md](ui-inventory.md)；**音量仍为"部分实现"**（生效路径已接通、当前无音源 ⇒ 听不到）；ImGui 默认字体无 CJK 字形 ⇒ 控件用中英双语标签 |
| G10 | **世界边界**：地图边缘必须有**不可穿越的边界**（空气墙），避免玩家掉出世界；边界由**地图范围自动推导**，对**测试地图与所有随机地图**一致生效 | **已实现** | `world/terrain/world_bounds.*`（由 tile 范围推导边界与墙位）+ `engine/physics/`（`AddStaticBox` 静态盒体）+ `game/out_of_bounds.hpp`（出界救援） | 墙按设计**完全不可见**；另配"掉出世界救援"（飞行越界后坠落送回出生点，余量 16 格），因为墙挡不住飞行 |

| G11 | **画面质量达到 3A 观感**：地表与物体表面**细腻圆润**、不发灰、有立体感与材质感（人工实测反馈"像 20 年前的游戏"） | **未开始** | 由 [ADR 0010](adr/0010-render-quality-pipeline.md) 定义的四阶段质量线承担：HDR + 色调映射（T20）→ 光照与阴影（T21）→ PBR 与材质四件套（T22）→ MSAA 与细节（T23）；判据采集靠 F1 面板性能数据（T24） | 该线属"纯增强"范畴，**由项目所有者于 2026-09-26 明确追加授权**；观感**必须人工目视验收**（SKILL 第七节），不以色带 / 阴影 / 高光 / 锯齿的可判定现象替代 |

| G12 | **不同元素的可破坏性不同**（项目所有者 2026-09-27 追加）：① **地形体量**（泥土 / 岩石…）按**材质坚固度**抵抗伤害 —— 泥土比岩石易挖；② **固定器物**（雕塑 / 机关 / 建筑构件 / 阵法设施）**几何不可变**（爆炸不在它身上挖洞），但**可被破坏**（状态 `完好 → 已破坏`），本阶段破坏后**只把外表整体变黑**以便区分 | **未开始**（规范已落盘） | 由 [ADR 0013](adr/0013-destructible-elements.md) 定义：`伤害 × 换算系数` 为体积预算，自爆心向外**逐格³ 扣减材质坚固度**（软的先被挖掉；不可破坏材质零改动）；器物走**状态切换 + 外观替换**（状态与表现分离）。配置：`materials.toml`(+`toughness`) / `projectiles.toml`(+`damage`) / `destruction.toml`(新)；落地约束见 `references/destructible-elements.md` | **数值待项目所有者确认**（光球伤害 10 / 泥土 3 / 岩石 5 已定，但**换算系数**与**器物破坏阈值**未定）；预制外观属**分期**（切换条件 = 美术资源就绪） |

### 2.3 主角与 NPC 能力清单（能力 · 状态 · 参数 · 落点）

> **为什么单列一节**：玩法能力会随阶段不断增加（跑 / 飞 / 破坏地形 / 收集 / 战斗……），
> 且**主角与 NPC 共用同一套底层能力**。这一节是**唯一的能力清单**，新增或改动任何能力都必须在此登记（见 SKILL 第六节六.7）。

**主角能力**

| 能力 | 状态 | 参数（当前值 / 来源） | 落点 |
| --- | --- | --- | --- |
| 走 / 冲刺 | **已实现** | 冲刺 20 格/秒；重力 24、跳跃初速 7.20（= 身高 60%）、最大坡度 50°、自动上台阶 1.0 格（`engine/physics/`） | `game/character_movement.hpp`、`engine/physics/` |
| 飞 | **部分实现** | 测试设施（`F` 开关、`Space` 升 / 左 `Ctrl` 降）；**正式形态（灵力 / 御剑等）待世界设定确认** | `game/main.cpp` |
| 破坏地形 | **部分实现** | **只由光球爆炸触发**（T27 起鼠标挖 / 堆笔刷已解绑）：命中**可挖体积**（标记区域内）→ 三维**平滑挖除球腔**（可掏出横向洞穴）；命中**区域外** → 地表爆破（`Crater`：坑体 + 外环、边界一阶连续）。参数：爆炸半径 / 深度 / 外环来自 `assets/config/projectiles.toml`；底层的 `Level` / `Crater` / `BrushFalloff` 仍在 `world/dig/terrain_brush.*`。**洞口可通行**（T28 起地表高度场在区域内让位于体积碰撞，见下表「进入可挖体积」）；**破坏后失去支撑的体会塌落**（T29，见下表「结构塌陷」）。**已知限制**：体积**无存档**（重进游戏洞会消失） | `world/dig/terrain_brush.*`、`world/dig/dig_volume.*`、`assets/config/projectiles.toml`、`game/main.cpp` |
| **进入可挖体积（洞口可通行）** | **已实现**（2026-09-27） | **「世界内一致性」审查判定为「破坏地形」的连带必做项**（T28 / [ADR 0012](adr/0012-collision-takeover-by-volumes.md)）：洞是**通道** ⇒ 角色**能走进去、能站在腔底**。做法：① 体积的物理碰撞 = 每块一个 Jolt 三角网体（`MeshShape`，实测 **137 个**）；② 角色碰撞交由体积接管 —— **被体积接管的 tile 不再建地表高度场碰撞**（实测 9 → **0** 个），否则隐形高度场会把角色挡在洞口外；③ 挖除后同步重建该块碰撞体。**冒烟实测**：角色落在体积面上，脚底高度与体积表面一致（120.00 格、着地=是）。**仍缺**：体积**无存档**（重进游戏洞会消失，用户 2026-09-27 明确"暂时不要求"）；部分覆盖的 tile（区域边界横穿）仍保留高度场 | `engine/physics/physics_world.*`、`world/dig/volume_collision.*`、`game/main.cpp` |
| **结构塌陷（支撑缺失 ⇒ 上部倒塌）** | **部分实现（首期）**（2026-09-27） | **「世界内一致性」审查判定为连带必做项**（T29 / [ADR 0012](adr/0012-collision-takeover-by-volumes.md) 第二节）：承重的梁 / 柱 / 地基被破坏后，上部结构**不得悬空不动**。**首期已落地**：爆炸后按「**载荷通路（纵向接地）+ 悬挑跨度**」判支撑，失去支撑的实心体**按连续段逐列下落**（**质量守恒**）并在腔底堆成带起伏的碎石（参数 `assets/config/collapse.toml`）。**未做（已登记）**：**连锁**复核（单次判定，不迭代）、**安息角 / 碎块刚体**、按材质区分强度；**建造层（ADR 0004 层 ③）尚未建立**，故当前只能塌"体积里的岩体"（例如把地标塔或山体立柱的根部轰断，其上段会整段落下），还不能塌"玩家盖的楼" | `world/dig/volume_collapse.*`、`assets/config/collapse.toml`、`game/main.cpp` |
| **发射弹丸（当前一种：光球）** | **已实现** | 鼠标左键 = 发射（按住按冷却连发，冷却 / 速度 / 重力系数 / 存活时间 / 半径 / 自发光色来自 `assets/config/projectiles.toml`，`[[projectile]]` 数组**留出多类型扩展**、当前只有 `light_orb`）；光球**受重力**影响走抛物线，**碰到物体表面即爆炸**；渲染为自发光球体 | `game/orb.*`、`game/projectile_table.*`、`assets/config/projectiles.toml`、`game/main.cpp` |
| **可破坏元素的分类与伤害结算** | **未开始**（规范已落盘） | **「不同元素可破坏性不同」（G12）的实现载体**（[ADR 0013](adr/0013-destructible-elements.md)）：**地形体量**按「材质**坚固度** × 伤害预算」**逐格³ 扣减**（软材质先被挖掉；不可破坏材质零改动）；**固定器物**几何不可变、状态 `完好 → 已破坏`（本阶段仅整体变黑占位）。数值：`materials.toml` 的 `toughness`、`projectiles.toml` 的 `damage`、新增 `destruction.toml` 的换算系数与器物阈值。**数值锚点**：光球（伤害 10）在纯泥土中挖出 **r ≈ 6 格** | [ADR 0013](adr/0013-destructible-elements.md)、`references/destructible-elements.md`、`world/dig/dig_volume.*`、`world/prop/`（新） |
| **结构整体性与刚体倒塌**（"规定哪些元素是一个整体"） | **未开始**（**能力缺口**；2026-09-27 第 7 轮识别）**方案已定**（同日确认 = **显式结构单元**，见 [ADR 0015](adr/0015-structure-units-and-rigid-collapse.md)：体素级 `structureId` + 地图预设声明 + 单元整体转刚体 + 落定回写为地形）；**待开工** | 现状塌落是**逐列独立**的（[ADR 0012](adr/0012-collision-takeover-by-volumes.md) 第二节）⇒ **原理上**只能"原地垂直下沉 / 局部陷落"，**无法**产生**倾斜、旋转、整体倾倒**。候选方案（须先开 ADR）：① **隐式连通分量** → 失支撑的实心体**整体**转 Jolt 动态刚体（塔被挖断后自然成为独立整体，**无需新数据通道**）；② **显式结构单元**（体素级 ID，可把"塔 A / 塔 B"各自规定为整体，需新数据通道与标记手段）；③ 把塔做成**器物**（层 ③ 预制件 + 预切碎块）——整体性天然存在，但几何不可变（**不能再被挖穿**）。**待确认**：塔能否被挖穿（决定走 ①/② 还是 ③） | 待定（ADR） |
| **洞内表面材质**（挖出的洞按"被切开的是什么材质"着色） | **已完成**（2026-09-27）：挖开草地看到**土**、挖开山体（陡坡 ⇒ 岩）看到**岩**；实现见 [ADR 0014](adr/0014-voxel-material-index.md)（顶点携带材质槽位覆盖；材质按列纯函数派生，**零额外内存**） | 现状：可挖体积的网格与地表**共用同一套材质规则**（`materials.toml` 的高度带 × 坡度带）⇒ 洞底（y≈110、坡度 0）命中"草"⇒ 绿色。**正解**：**体素级材质**（每体素一个材质索引），内表面按被切开的材质着色。**与「可破坏元素的分类与伤害结算」共用同一前置（体素级材质）**，建议合并实现 | `world/dig/dig_volume.*`、`assets/config/materials.toml`、`assets/shaders/mesh.frag`（待开 ADR） |
| 收集物品 | **未开始** | 缺：物品定义、拾取交互、背包 / 持有 | 待登记 |
| NPC 能力（复用同一套） | **未开始** | 与主角共用移动 / 地形改造底层能力；**是否允许 NPC 改造地形、以何种规则**待 NPC 阶段确认 | 见 [npc-behavior.md](npc-behavior.md) |

**通用约定**

- **能力清单里的每一条都必须可判定**（有参数或可观察现象），不得只写"支持 X"。
- **主角与 NPC 的能力差异**（数值、感知、权限）在 NPC 阶段写明；底层能力不各写一套。
- **连带必做项（2026-09-27 起）**：按 SKILL「世界内一致性」节，**"用户明确提出的破坏 / 交互"所必然带来的连带后果，属于该需求的一部分**
  （例：挖了洞 ⇒ 洞要能进去；承重被毁 ⇒ 上部要塌）。本表里带 **"连带必做项"** 字样的行就是由该审查识别出来、
  但**尚未实现**的项；**它们的降级或分期交付必须先经项目所有者确认**，不得默默计入"遗留"。

### 2.4 明确不在当前阶段（防范围蔓延）

- 战斗、任务、对话、交易、合成、建造、多人在线、成就与存档。
- 以上若要做，须先在此登记并给出可判定判据，再排期。
- 引擎侧"暂缓项"清单见 SKILL 第五节（场景编辑器、脚本系统、联机、多后端、自研 ECS）。

## 3. 主角（玩家角色）

| 项 | 结论 | 状态 |
| --- | --- | --- |
| 技术载体 | 胶囊角色 + 第三人称相机（引擎能力） | **已实现** |
| 外观模型 | **程序化胶囊占位体已可见**（沙黄色，尺寸同碰撞胶囊 0.30 / 1.80）；正式模型与美术风格**待补**（是否用公共资源库模型） | **部分实现** |
| 身份 / 背景 | 待补 —— **须由项目所有者提供**（AI 不得代拟） | 未开发 |
| 成长体系（境界、功法、属性） | 待补 —— 须由项目所有者提供 | 未开发 |
| 初始能力（跑 / 飞 / 收集 / 破坏地形） | 见 §2.2 | 部分实现 |
| **跳跃高度** | **= 角色身高的 60%**（身高 1.80 格 → **1.08 格**）；由"身高 + 重力"**推导**初速（当前 `sqrt(2·24·0.6·1.80) = 7.20`），改身高时跳跃自动跟随 | **已实现**（2026-09-25） |

## 4. 与其它文档的分工

| 问题 | 写在哪 |
| --- | --- |
| 引擎有没有这个能力 | [engine-capabilities.md](engine-capabilities.md) |
| 本游戏要什么、当前阶段做到哪 | 本文件 |
| 世界是什么样（题材细节 / 地理 / 种族 / 历史 / 命名） | [world-setting.md](world-setting.md) |
| NPC 怎么想、怎么动 | [npc-behavior.md](npc-behavior.md) |
| 为什么这么选（技术方案） | `docs/adr/` |
| 什么时候做、做完算不算过 | `docs/plans/<阶段>.md` |

## 5. 登记规则

1. **先登记后动手**：任何新玩法需求先在 §2 补一行（状态 `未开始`），再实现，完成后回填。
2. **可判定**：每行的"实现方式 / 落点"必须具体到模块或文件；写"用某个系统实现"这类空话视为未填。
3. **不写"A 或 B"**：涉及技术选型的（如物品系统怎么存、飞行的实现方式），候选与理由写 **ADR**。
4. **不虚构设定**：主角身份、境界体系、门派、地名一律来自项目所有者，未提供就标 `待补`。
5. **需求与引擎能力联动**：需求若依赖引擎缺失的能力，必须同时在 `engine-capabilities.md` 占一行，
   并在阶段计划里给出实现任务。

## 6. 待项目所有者确认的问题

> 回答后整理进上表，并在此标记已答。

1. **世界尺度**：一个世界大约多大（多少平方公里 / 多少 tile）？是单一大陆还是多区域？
2. **随机程度**：每次随机生成时，哪些东西必须保持稳定（如"一定有某类地貌"）？→ 对应 G2。
3. **必备元素清单**：`G2` 具体要声明哪些内容？（至少举 2~3 个例子，如"每张地图必须有一条河""必须有可采集的灵石"）
4. **飞行的定位**：是开局即可自由飞，还是需要条件（境界 / 道具）？→ 决定 G5 的做法与手感
5. **收集物范围**：`G7` 要收集什么（资源 / 装备 / 功法 / 材料）？收集后如何使用？
6. **主角设定**：身份、境界体系、成长方式（是否由项目所有者提供，或需要一起设计？）
